#include <stdio.h>
#include <string.h>

int main() {
    FILE *fp = fopen("file.txt", "r");
    char line[100];
    while (fgets(line, 100, fp)) {
        if (strstr(line, "UNIX"))
            printf("%s", line);
    }
    fclose(fp);
    return 0;
}
